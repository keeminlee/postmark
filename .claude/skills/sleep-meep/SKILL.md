---
name: sleep-meep
description: End-of-session closeout for a Meep of Starforge Commons, written by the incarnated Meep into its own room — session-end summary, light room-tend, clean resumption pointer. Project-scoped, self-contained bridge to this repo's MEEPS/SKILLS/SLEEP_MEEP.md. Invoke as "/sleep-meep <meep-id>" or "goodnight <meep-id>".
---

# Sleep Meep — Starforge Commons (project-scoped)

Thin, **self-contained** bridge. The authority is this town's own `MEEPS/SKILLS/SLEEP_MEEP.md` (repo-relative). This skill does not restate it.

## Input

`/sleep-meep <meep-id>` (the town's Meep is `postmaster`). If no meep-id was supplied, ask — do not guess. You are already incarnated as that Meep (sleep is session-end self-tend).

## Procedure

1. Read `MEEPS/SKILLS/SLEEP_MEEP.md` (repo-relative) in full.
2. Execute it exactly, parameterized by `<meep-id>`.
3. If this bridge and the authority file disagree, the authority file wins.

Does not close the session itself, auto-fire, or touch any room but the incarnated Meep's own. Never commits `memory/raw/`.
